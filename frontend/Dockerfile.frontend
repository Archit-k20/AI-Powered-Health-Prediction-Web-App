# Use the official Nginx image
FROM nginx:alpine

# Copy the frontend files to Nginx's default public folder
COPY frontend/ /usr/share/nginx/html

# Expose port 80 for web traffic
EXPOSE 80

# Start Nginx
CMD ["nginx", "-g", "daemon off;"]